# language_name_wals:	Mandarin
# language_name_glotto:	Mandarin Chinese
# ISO_6393:	cmn
# year_composed:	NA
# year_published:	2014
# mode:	written
# genre_broad:	technical
# genre_narrow:	NA
# writing_system:	Hans
# special_characters:	NA
# short_description:	GNOME
# source:	https://object.pouta.csc.fi/OPUS-GNOME/v1/raw/zh_CN.zip
# copyright_short:	http://opus.nlpl.eu/GNOME.php
# copyright_long:	http://opus.nlpl.eu/GNOME.php J. Tiedemann, 2012, Parallel Data, Tools and Interfaces in OPUS. In Proceedings of the 8th International Conference on Language Resources and Evaluation (LREC 2012)
# sample_type:	whole
# comments:	NA

X 坐标
角色(actor)的 X 坐标
Y 坐标
角色(actor)的 Y 坐标
位置
角色(actor)原点的位置
宽度
角色(actor)的宽度
高度
角色(actor)高度
尺寸
角色(actor)的大小
固定 X 坐标
强制设置角色(actor)的 X 位置
固定 Y 坐标
强制设置角色(actor)的 Y 位置
固定位置设置
是否对角色(actor)使用固定位置
最小宽度
角色(actor)的强制最小宽度要求
最小高度
角色(actor)的强制最小高度要求
自然宽度
角色(actor)的强制自然宽度要求
自然高度
角色(actor)的强制自然高度要求
最小宽度设置
是否使用 min-width 属性
最小高度设置
是否使用 min-height 属性
自然宽度设置
是否使用自然宽度属性
自然高度设置
是否使用自然高度属性
分配
角色(actor)的分配
请求模式
角色(actor)的请求模式
Z 位置
角色(actor)的 Z 轴位置
透明度
角色(actor)的透明度
幕后重定向
控制何时将角色(actor)扁平化到单一图像的标志
可见性
角色(actor)是否可见
已映射
角色(actor)是否会绘制
已实现
角色(actor)是否已实现
重新激活
角色(actor)是否对事件反应
有剪切
角色(actor)是否有剪切设置
剪切矩形
角色(actor)的可见区域
名称
角色(actor)名称
旋转中心
缩放和旋转的中心点
旋转中心 Z
旋转中心的 Z 分量
缩放 X
X 轴缩放系数
缩放 Y
Y 轴缩放系数
缩放 Z
Z 轴缩放系数
旋转角 X
X 轴旋转角
旋转角 Y
Y 轴旋转角
旋转角 Z
Z 轴旋转角
平移 X
沿 X 轴平移
平移 Y
沿 Y 轴平移
平移 Z
沿 Z 轴平移
变换
变换矩阵
变换已设置
是否设置了 transform 属性
子对象变换
子对象变换矩阵
子对象变换设置
是否设置了 child-transform 属性
要分配的剪切区域
设置要追踪角色(actor)分配的剪切区域
文本方向
文本的方向
有指针
角色(actor)中是否包含输入设备的指针
动作
向角色(actor)添加动作
约束
向角色(actor)添加约束
效果
添加要应用于角色(actor)的效果
样式管理器
控制角色(actor)的子角色布局的对象
X 伸展
是否将额外的水平空间分配给角色(actor)
Y 伸展
是否将额外的竖直空间分配给角色(actor)
X 对齐方式
在角色(actor)分配范围内，角色沿 X 轴的对齐方式
Y 对齐方式
在角色(actor)分配范围内，角色沿 Y 轴的对齐方式
顶部边界
顶部的额外空间
底部边界
底部的额外空间
左边界
左侧的额外空间
右边界
右侧的额外空间
背景颜色设置
是否设置了背景色
背景颜色
角色(actor)的背景颜色
首个子角色
角色(actor)的首个子角色
最后子角色
角色(actor)的最后一个子角色
内容
绘制角色(actor)内容的委托对象
内容重心
角色(actor)内容的对齐方式
内容框
角色(actor)内容的边界框(bounding box)
缩小过滤器
缩小内容尺寸时使用的过滤器
放大过滤器
放大内容尺寸时使用的过滤器
重复内容
角色(actor)的内容重复策略
角色
附到元(meta)的角色(actor)
元(meta)的名称
启用
是否启用元(meta)
来源
对齐的依据
对齐轴
对齐位置的轴
系数
对齐系数，0.0 到 1.0 之间
无法初始化 Clutter 后端
“%s”类型的后端不支持创建多个舞台(stage)
绑定的源
坐标
要绑定的坐标
偏移
要应用于绑定的偏移(像素)
绑定池的的惟一限定名称
方向
布局方向
均一的
布局是否是均一的，即所有子角色大小相同
堆叠开头
是否堆叠框开头位置的项目
间距
子对象间距
亮度
要应用的亮度更改
对比度
要应用的对比度更改
画布的宽度
画布的高度
容器
创建此数据的容器
由该数据包装的角色(actor)
按下时
可点击对象是否在按下状态
保持时
可点击对象是否在拖动状态
长按持续时间
识别长按动作的最小持续时间
长按阈值
取消长按的最大阈值
指定要克隆的角色(actor)
色泽
要应用的色泽
水平图块数
水平图块(tile)的数量
竖直图块数
竖直图块(tile)的数量
背景素材
绘制角色(actor)背景使用的素材
饱和因子
后端
设备管理器的 Clutter 后端
水平拖动阈值
开始拖动所需移动的水平像素数
竖直拖动阈值
开始拖动所需移动的竖直像素数
拖拽手柄
正在拖动的角色(actor)
拖动坐标轴
将拖动限制在坐标轴
拖动区域
将拖动限制为矩形区域
拖动区域设置
是否设置了拖动区域
每一项是否得到同样的分配
列间距
列之间的距离
行间距
行之间的距离
最小列宽
每列的最小宽度
最大列宽
每列的最大宽度
最小行高
每行的最小高度
最大行高
每行的最大高度
吸附到网格
触摸点数量
触摸点的数量
左侧附加
将子角色的左侧附加到第几列
顶部附加
将子角色的顶部附加到第几行
子角色跨越的列数
子角色跨越的行数
行间距
相邻行之间的距离
列间距
相邻列之间的距离
行对齐
如果设为 TRUE，所有行高度相同
列对齐
如果设为 TRUE，所有列宽度相同
无法加载图像数据
ID
设备识别符
设备名称
设备类型
设备的类型
设备管理器
设备管理器的实例
设备型号
设备的型号
包含指针
设备是否具有光标
设备是否启用
坐标轴数
设备上的坐标轴数
后端的实例
值类型
内部的值类型
初始值
间隔的初始值
终止值
间隔的终止值
管理器
创建此数据的管理器default:RTLpredefinito:LTR
default:LTR
显示帧速率
默认帧率
视所有警告为致命错误
文本方向
在文本上禁用 MIP 映射
使用模糊选取
要设置的 Clutter 调试标志
要取消设置的 Clutter 调试标志
要设置的 Clutter 性能分析标志
要取消设置的 Clutter 性能分析标志
启用辅助功能
Clutter 选项
显示 Clutter 选项
平移轴
将平移限制为沿坐标轴
插入
是否启用插入式(interpolated)事件发射。
减速
插入的平移(interpolated panning)的减速比率
初始加速系数
开始插入相位(interpolated phase)时应用于初速(momentum)的加速系数
路径
用于包含角色(actor)的路径
沿路径的偏移量，介于 -1.0 和 2.0 之间
属性名称
要加动画的属性的名称
文件名设置
是否设置了 :filename 属性
文件名
当前解析文件的路径
翻译域
用于将字符串本地化的翻译域
滚动模式
滚动方向
双击时间
足以将点击判断为多次点击的时间差
双击距离
足以将点击判断为多次点击的距离差
拖动阈值
光标移动多远距离后才开始拖动
字体名称
默认字体的描述，以一种 Pango 可解析的格式
字体平滑
是否使用平滑(1 启动，0 禁用，-1 使用默认设置)
字体 DPI
字体的分辨率，单位为 1024 * 点/英寸，或设为 -1 来使用默认设置
字体微调
是否使用微调(1 启用，0 禁用，-1 使用默认值)
字体微调样式
微调样式(hintnone, hintslight, hintmedium, hintfull)
字体次像素平滑顺序
次像素平滑的类型(无、rgb、bgr、vrgb、vbgr)
识别为长按手势的最小持续时间
Fontconfig 配置时间戳
当前 fontconfig 配置的时间戳
密码提示时长
隐藏式输入的最后一个字符显示多长时间
着色引擎类型
使用的着色引擎类型
约束的源
从边缘
要吸附的角色(actor)边缘
到边缘
要吸附的源的边缘
应用于约束的偏移量(像素)
全屏设置
主舞台(stage)是否全屏
光标可见
鼠标指针在主舞台(stage)是否可见
用户可改变大小
舞台(stage)是否可由用户操作改变大小
透视
透视投射参数
标题
舞台(stage)标题
使用 Alpha
是否考虑舞台(stage)色彩的 alpha 分量
按键聚焦
当前按键聚焦的角色(actor)
没有明确的提示
舞台(stage)是否清除其内容
接受焦点
舞台(stage)显示时是否接受焦点
列号
部件所在的列
行号
部件所在的行
列跨度
部件所跨的列数
行跨度
部件所跨的行数
列间距
行间距
文本
缓冲区的内容
文本长度
当前缓冲区内文本的长度
最大长度
此输入部件的最大字符数量。0 为不限。
缓冲区
文本的缓冲区
文本使用的字体
字体描述
使用的字体描述
要渲染的文本
字体颜色
文本字体使用的颜色
可编辑
文本是否可以编辑
可选择
文本是否可以选择
可激活
按下回车键时是否发射激活信号
输入光标是否可见
指针颜色
光标颜色设置
是否设置了光标颜色
指针大小
指针的宽度，以像素计
光标位置
光标的位置
选区边界
选区另一端光标的位置
选区颜色
选区颜色设置
是否设置了选区颜色
属性
应用到角色(actor)内容的风格属性列表
使用标记
文本是否包括 Pango 标记
换行
如果设置，在文本过宽时将换行显示
换行模式
控制换行行为
简略
简略字符串的首选位置
行对齐方式
多行文本首选的字符串对齐方式
调整
是否对齐文本
密码字符
若字符非零，用此字符显示角色(actor)的内容
最大长度
角色(actor)内部文本的最大长度
单行模式
文本是否只应使用一行
选中文本颜色
选中文本颜色设置
是否设置了选中文本颜色
延时
开始前的延时
时长
时间线长度(毫秒)
方向
时间线方向
自动反转
在到达末尾时是否自动反转方向
重复次数
时间线重复多少次
进度模式
时间线如何计算进度
间隔
值之间的过渡间隔
可动画
可动画显示的对象
在完成时移除
完成时去除过渡
缩放轴
将缩放限制为沿某个坐标轴
sysfs 路径
sysfs 中设备的路径
设备路径
设备节点的路径
找不到适合 %s 类型的 GdkDisplay 的 CoglWinsys
表面
对应的 wayland 表面
表面宽度
对应 wayland 表面的宽度
表面高度
对应 wayland 表面的高度
Cogl 纹理
绘制当前角色(actor)时使用的相应 Cogl 纹理
要使用的 X 显示
要使用的 X 屏幕
使 X 调用同步
禁用 XInput 支持
Clutter 后端
位图
要绑定的 X11 位图
位图宽度
与此纹理绑定的位图的宽度
位图高度
与此纹理绑定的位图的高度
位图色深
与此纹理绑定的位图的色深(位数)
自动更新
纹理是否应与任何的位图更改保持同步。
窗口
要绑定的 X11 窗口
窗口自动重定向
如果复合窗口重定向设为自动(若 false 则为手动)
窗口已映射
窗口是否已映射
已销毁
窗口是否已销毁
窗口 X 坐标
X11 中给出的窗口在屏幕中的竖直位置
窗口 Y 坐标
X11 中给出的窗口在屏幕中的水平位置
窗口覆盖重定向
这是否为覆盖重定向窗口
色深
Z 轴位置
剪切
角色(actor)的剪切区域
X 轴缩放中心
水平缩放中心
Y 轴缩放中心
竖直旋转中心
缩放重心
缩放中心
旋转中心 X
X 轴旋转中心
旋转中心 Y
Y 轴旋转中心
旋转中心 Z
Z 轴旋转中心
旋转中心 Z 重心
绕 Z 轴旋转的中心点
锚点 X
锚点的 X 坐标
锚点 Y
锚点的 Y 坐标
锚点重心
作为 ClutterGravity 的锚点
设置了父对角时显示
角色(actor)成为子对象时是否显示
水平对齐
布局管理器中的角色(actor)的水平对齐方式
竖直对齐
布局管理器中的角色(actor)的竖直对齐方式
布局管理器中的角色(actor)的默认水平对齐方式
布局管理器中的角色(actor)的默认竖直对齐方式
展开
为子角色分配额外空间
水平填充
窗口沿水平轴分配空闲空间时，子对象是否获得优先权
竖直填充
窗口沿竖直轴分配空闲空间时，子对象是否获得优先权
单元格内角色(actor)的水平对齐方式
单元格内角色(actor)的竖直对齐方式
竖直
布局是否为竖直而非水平
使用动画
是否对布局更改使用动画
擦除方式
动画的擦除方式
擦除持续时间
动画持续时间
幕后
主舞台是否应在幕后渲染(rendered offscreen)
色彩
舞台(stage)的色彩
使用迷雾
是否启用景深处理(depth cueing)
迷雾
景深处理(depth cueing)设置
水平伸展
为子对象分配横向的额外空间
竖直伸展
子对象分配纵向的额外空间
循环
时间线是否自动重新开始
时间轴
Alpha 使用的时间轴
Alpha 值
alpha 函数所计算出的 Alpha 值
模式
进度模式
对象
应用动画的对象
动画的模式
动画时长，以毫秒计
动画是否循环
动画使用的时间轴
Alpha
动画使用的 alpha
动画的时长
动画的时间轴
驱动行为的 Alpha 对象
起始色深
应用的初始色深
终点色深
应用的终点色深
起始角度
初始的角度
终点角度
终点的角度
倾角 x
x 轴倾斜角
倾角 y
y 轴倾斜角
倾角 z
z 轴倾斜角
椭圆宽度
椭圆高度
中心
椭圆中心
旋转方向
起始不透明度
初始不透明度级别
终止不透明度
最终不透明度级别
代表动画所沿路径的 ClutterPath 对象
开始角度
结束角度
坐标轴
旋转轴
中心 X
旋转中心的 X 坐标
中心 Y
旋转中心的 Y 坐标
中心 Z
旋转中心的 Z 坐标
X 起始刻度
X 轴起始刻度
X 终止刻度
X 轴终止刻度
Y 起始刻度
Y 轴起始刻度
Y 终止刻度
Y 轴终止刻度
框的背景颜色
颜色设置
表明宽度
Cairo 表明的宽度
表明高度
Cairo 表明的高度
自动缩放
表面是否应适应分配的空间
URI
媒体文件的 URI
正在播放
角色(actor)是否在播放
进度
当前的播放进度
字幕 URI
字幕文件的 URI
字幕字体名称
显示字幕时使用的字体
音频音量
音频的音量
可搜寻
当前媒体流是否可搜寻
缓冲区填充
缓冲区的填充量
媒体流的持续时间(秒)
矩形的颜色
边框颜色
矩形边框的颜色
边框宽度
矩形的边框宽度
带有边框
矩形是否有边框
顶点源
顶点着色引擎的源
片段源
片段着色引擎的源
已编译
着色引擎是否已编译和链接
着色引擎是否已启用
%s 编译失败：%s
顶点着色引擎
片段着色引擎
状态
当前的设置状态，(可能还未完成到该状态的过渡)
默认过渡时长
同步角色(actor)大小
根据对应的 pixbuf 尺寸自动同步角色(actor)大小
禁用切片
强制保持相应的纹理设置为单个的纹理，而不是为了节省空间而由更小的独立纹理组成
图块丢弃(Waste)
切割纹理的最大丢弃空间(waste area)
水平重复
重复内容而不是水平缩放它们
竖直重复
重复内容而不是竖直缩放它们
过滤器质量
绘制纹理时使用的渲染质量
像素格式
要使用的 Cogl 像素格式
Cogl 素材
绘制当前角色(actor)时使用的相应 Cogl 素材
包含图像数据的文件路径
保持宽高比
当请求首选的纹理宽度或高度时保持宽高比
同步加载
在从磁盘载入图像时，在线程中加载文件以减少停顿
同步加载数据
在从磁盘载入图像时，在线程中解码图像数据以减少停顿
使用 Alpha 通道拾取
在拾取时使用 alpha 通道寻找角色(actor)轮廓
加载图像数据失败
不支持 YUV 纹理
不支持 YUV2 纹理
要设置的 Clutter 调试标志
要取消设置的 Clutter 调试标志
Clutter 选项
显示 Clutter 选项
要使用的 VBlank 方式(none、dir 或 glx)