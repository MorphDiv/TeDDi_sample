# language_name_wals:	Mandarin
# language_name_glotto:	Mandarin Chinese
# ISO_6393:	cmn
# year_composed:	NA
# year_published:	2014
# mode:	written
# genre_broad:	technical
# genre_narrow:	NA
# writing_system:	Hans
# special_characters:	NA
# short_description:	GNOME
# source:	https://object.pouta.csc.fi/OPUS-GNOME/v1/raw/zh_CN.zip
# copyright_short:	http://opus.nlpl.eu/GNOME.php
# copyright_long:	http://opus.nlpl.eu/GNOME.php J. Tiedemann, 2012, Parallel Data, Tools and Interfaces in OPUS. In Proceedings of the 8th International Conference on Language Resources and Evaluation (LREC 2012)
# sample_type:	whole
# comments:	NA

期望一个 JSON 对象，但根节点类型是“%s”
JSON 节点中存在异常的类型“%s”
在 JSON 数组中缺少用来构成 tuple(元组) 的元素
在 GVariant 元组类型中缺少闭合符号“)”
在 JSON 数组中存在意外的其他元素
字符串值到 GVariant 的转换无效
GVariant 字典记录期望仅有一个成员的 JSON 对象
不支持的 GVariant 类“%c”
无效的 GVariant 签名
JSON 数据为空
%s:%d:%d：解析错误：%s
JSONPath 表达式中只允许有一个根节点
根节点之后跟随了无效的字符“%c”
有错误的 slice 表达式“%*s”
无效的 set 定义 '%*s'
无效的 slice 定义“%*s”
无效的数组索引定义“%*s”
当前节点的类型是“%s”，应为数组或对象。
索引“%d”大于当前位置数组的大小。
索引“%d”大于当前位置对象的大小。
当前位置无节点可用
当前位置是一个“%s”，而不是一个数组
当前节点的类型是“%s”，但应为对象。
成员“%s”没有在当前位置的对象中定义。
当前位置是一个“%s”，而不是一个对象
当前位置是一个“%s”，不是一个值
当前位置不是一个字符串类型
当前位置不是一个整数类型
当前位置不是一个浮点数类型
当前位置不是一个布尔(boolean)类型